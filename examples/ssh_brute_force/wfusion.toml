sources_dir = "topology/sources"
windows = "windows.toml"
sinks = "topology/sinks"

[runtime]
executor_parallelism = 2
rule_exec_timeout = "30s"
schemas = "schemas/*.wfs"
rules   = "rules/ssh_brute_force.wfl"

[logging]
level = "info"
format = "plain"
