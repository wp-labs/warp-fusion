use "network.wfs"
rule simple {
    events { c : conn_events }
    match<sip:5m> {
        on event { c | count >= 1; }
        and close { total: c | count >= 1; }
    } -> score(70.0)
    entity(ip, c.sip)
    yield network_alerts (sip = c.sip, alert_type = "test", detail = "test")
}
