[[connectors]]
allow_override = ["brokers", "topic", "fmt", "protocol", "num_partitions", "replication", "config"]
id = "kafka_sink"
type = "kafka"

[connectors.params]
brokers = "localhost:9092"
fmt = "json"
num_partitions = 1
protocol = "text"
replication = 1
topic = "wp_events"
